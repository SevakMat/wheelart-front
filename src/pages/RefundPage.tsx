import RefundContainer from "../components/refundContainer/RefundContainer";
import MainContainer from "../hok/mainContainer/MainContainer";

const RefundPage = (): JSX.Element => {
  return (
    <MainContainer>
      <RefundContainer />
    </MainContainer>
  );
};
export default RefundPage;
