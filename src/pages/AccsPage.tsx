import MainContainer from "../hok/mainContainer/MainContainer";
import AccsContainer from "../components/accsContainer/AccsContainer";

const AccsPage = () => {
  return (
    <MainContainer>
      <AccsContainer />
    </MainContainer>
  );
};

export default AccsPage;
