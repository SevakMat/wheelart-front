import MainContainer from "../hok/mainContainer/MainContainer";
import TiresContainer from "../components/tiresContainer/TiresContainer";

const TiresPage = () => {
  return (
    <MainContainer>
      <TiresContainer />
    </MainContainer>
  );
};

export default TiresPage;
